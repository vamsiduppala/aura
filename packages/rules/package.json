{
  "name": "@vim/rules",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Astrologer-editable scoring tables (data) plus the deterministic Plan Composer (mechanism).",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./json": "./rules.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aura/engine": "*",
    "@aura/knowledge": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
